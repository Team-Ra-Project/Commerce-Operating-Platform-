package com.rastudio.commerce.messaging;

/** Matches the `channel` ENUM('WHATSAPP','EMAIL') used by message_template, campaign_step, and message_log. */
public enum MessageChannel { WHATSAPP, EMAIL }
